HOW TO RUN THE CODE:


to compile input the following commands:

gcc -o FA FA.c Finiteautomata.c
gcc -o Finiteautomata Finiteautomata.c FA.c

then run the executable:

./FA

(to change the values and modify the input you need to modife the FA.c file)
